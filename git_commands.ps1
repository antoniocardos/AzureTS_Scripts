#add file to queue
#git add <file>

#see files on queue
git status

#Clone remote repository
git clone username@host:/path/to/repository

#commit files
git commit –m “Message to go with the commit here”

#if push is requesting username and password
git remote set-url origin git remote set-url origin git@github.com:antoniocardos/AzureTS_Scripts.git
git remote -v